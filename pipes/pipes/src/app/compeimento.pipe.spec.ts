import { CompeimentoPipe } from './compeimento.pipe';

describe('CompeimentoPipe', () => {
  it('create an instance', () => {
    const pipe = new CompeimentoPipe();
    expect(pipe).toBeTruthy();
  });
});
